import 'package:supabase_flutter/supabase_flutter.dart';

void main() {
  final client = Supabase.instance.client;
  // This is a dummy script to check if this compiles
  // client.auth.getOAuthSignInUrl(provider: OAuthProvider.google);
}
